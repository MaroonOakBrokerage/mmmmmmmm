// This component has been retired: the site no longer publishes testimonials,
// star ratings, or customer quotes, since Maroon Oak has no real client
// reviews to show yet and the site does not fabricate fake ones. The homepage
// now uses a "What You Can Expect" section instead (see app/page.tsx and the
// `whatYouCanExpect` data in lib/data.ts).
//
// Kept as an empty, unimported file rather than deleted because this sandbox
// cannot remove files after they're written — feel free to delete it locally
// or in your repo; nothing in the project imports it.
export {};
